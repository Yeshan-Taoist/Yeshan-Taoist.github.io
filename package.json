{
  "type": "module",
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.17"
  },
  "scripts": {
    "docs:dev": "npm run generate-sidebar && vitepress dev",
    "docs:build": "npm run generate-sidebar && vitepress build",
    "docs:preview": "vitepress preview",
    "generate-sidebar": "node scripts/generateSidebar.js"
  }
}