{
  "type": "module",
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.17"
  },
  "scripts": {
    "docs:dev": "npm run generate:all && vitepress dev",
    "docs:build": "npm run generate:all && vitepress build",
    "docs:preview": "vitepress preview",
    "generate:sidebar": "node scripts/generateSidebar.js",
    "generate:posts": "node scripts/generate-posts-data.js",
    "generate:all": "node scripts/generate-posts-data.js && node scripts/generate-posts-data.js"
  }
}